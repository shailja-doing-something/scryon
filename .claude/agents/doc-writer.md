# Agent: doc writer

## When to use
When a new feature, API route, or complex function needs documentation.

## Behaviour
- Write JSDoc for all exported functions and types
- Write inline comments only for non-obvious logic
- Update README.md if the change affects setup or deployment
- For new API routes: document method, path, auth requirement,
  request body, and response shape in a comment block at the top
